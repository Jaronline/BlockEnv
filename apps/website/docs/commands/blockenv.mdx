import BlockEnvHelp from "./_generated_help/blockenv.md";

# blockenv

<BlockEnvHelp />

## Examples

Display the help message for the `blockenv` command:

```bash
blockenv --help
```

Show the BlockEnv version information:

```bash
blockenv --version
```
